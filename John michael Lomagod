I was the one who created the main folder in VS Code and initialized the Git repository using Git Bash. I also created the README.md file and wrote the explanation about the folder structure and component layering strategy. After that, I helped my groupmates on how to add, commit, and push their files to GitHub. I also checked if the repository was already public so our instructor can see it.
